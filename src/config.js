// constantes (velocidades, tamanhos, cores)

// Configurações de combate
export const ENEMY_SPAWN_DELAY = 2000; // 2 segundos de delay antes dos inimigos atacarem

// Configurações de fire rate do player
export const BASE_FIRE_RATE = 300; // Fire rate base (ms entre tiros)
export const MIN_FIRE_RATE = 100; // Fire rate mínimo (mais rápido possível)
